server:
  port: 8080
spring:
  application:
    name: "ConsoleLisAwx"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8080"

webclient:
  bass-url: "http://192.168.30.33:10445/api/v2/"
